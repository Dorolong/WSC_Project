# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트에서 세션을 시작할 때 항상 읽는
프로젝트 지시사항 파일이다.

## 작업 환경 변경 (컴퓨터 간 이어서 작업하기)

이 프로젝트는 여러 컴퓨터를 오가며 작업한다. `progress/`,
`debug_logs/`, `README.md`가 컴퓨터 간 이어서 작업하기 위한 유일한
전달 수단이다(Claude의 대화 메모리는 컴퓨터를 안 따라옴 - 반드시
이 파일들에 다 남겨야 함).

### 사용자가 "작업 환경 변경"이라고 말하면 (다른 컴퓨터로 옮기기 전, 저장)

지금까지의 세션 내용을 다른 컴퓨터에서 그대로 이어갈 수 있도록
다음을 수행한다:

1. 이번 세션에서 완료된 작업을 `progress/NN_주제.txt` 형식으로
   새 파일에 정리(기존 파일 스타일 참고 - 배경/구현/버그/결과/상태
   섹션 구조).
2. 아직 안 끝난 작업(설계만 되고 코드 없음, 작성 중 중단된 코드
   등)은 정확히 어디까지 됐는지, 다음에 뭘 해야 하는지 구체적으로
   남긴다 - "무엇을", "어느 파일 몇 번째 줄부터", "왜 아직 안
   끝났는지"까지.
3. `progress/` 폴더의 백로그 파일(가장 큰 번호, "향후 개선 과제
   백로그" 파일)을 갱신 - 완료된 항목은 지우고 새 진행상황을
   반영해 **다음 번호로 새로 저장**, 이전 버전은 삭제. 이 파일의
   "다른 컴퓨터/세션에서 이어서 시작할 때 여기부터 읽을 것" 섹션이
   가장 중요 - 지금 당장 다음에 뭘 해야 하는지가 여기 정확히 있어야
   함.
4. 코드에서 새로 발견했지만 아직 안 고친 버그가 있으면 백로그와
   해당 파일 상태 문서(`progress/07`, `08` 등 파일별 현재상태
   문서)에 "알려진 버그"로 명시.
5. `README.md`의 "현재 상태"와 "할 일" 섹션을 최신 상태로 갱신
   (`progress/`보다 먼저 읽히는 요약본이라 반드시 같이 갱신).
6. 필요하면 `debug_logs/(YYYY-MM-DD)_주제.txt` 형식으로 세부
   디버깅 과정(문제→원인→해결→상태)도 기록.
7. 프로젝트 폴더 전체를 `.venv`와 모든 `__pycache__` 폴더만 제외하고
   압축해서 `WSC_DriveEff_Project_handoff.zip`으로 프로젝트 폴더의
   **부모 디렉터리**(바탕화면)에 저장한다 (`.venv`는 SETUP.md에
   따라 새 컴퓨터에서 `requirements.txt`로 재현하는 게 맞고, 옮기면
   그 컴퓨터 경로/아키텍처가 달라 깨짐 - 압축 대상에서 항상 제외).
   robocopy로 임시 스테이징 폴더에 `.venv`/`__pycache__` 제외 복사 후
   `Compress-Archive`로 압축하는 방식 사용(Compress-Archive 자체엔
   재귀적 디렉터리 제외 옵션이 없어서 스테이징이 필요). CSV 등 파일이
   외부 프로그램에 열려 있으면 EPERM으로 일부 파일이 누락될 수 있으니,
   압축 후 항목 수를 실제 파일 수(`.venv`/`__pycache__` 제외)와
   대조해서 누락이 없는지 확인하고, 누락 있으면 사용자에게 파일을
   닫아달라고 요청 후 재압축한다.

### 사용자가 "작업 환경 변경 완료"라고 말하면 (다른 컴퓨터에서 열자마자, 불러오기)

1. `README.md`의 "현재 상태"/"할 일" 섹션을 먼저 읽는다.
2. `progress/` 폴더에서 **가장 큰 번호**의 파일(백로그)을 읽고,
   그 안 "다른 컴퓨터/세션에서 이어서 시작할 때 여기부터 읽을 것"
   섹션을 확인한다.
3. 거기서 언급된 관련 `progress/NN_주제.txt` 파일들(진행중 작업의
   상세 설계/스펙이 담긴 파일)을 추가로 읽는다.
4. 위 내용을 종합해서 지금 정확히 어디까지 됐고 다음에 뭘 해야
   하는지 사용자에게 간단히 요약 보고하고, 바로 이어서 작업을
   시작한다 - 사용자에게 다시 설명해달라고 요청하지 않는다.

## 코드 작성 주체 (중요)

`Functions/Vehicle_Function.py`와 `mpc/mpc_controller.py`에 들어가는
의사결정/물리 로직은 사용자가 직접 작성한다. Claude는 공식/함수
시그니처/후크 지점 스펙을 텍스트로 제공하되, 이 두 파일에
Edit/Write를 직접 실행하지 않는다 - 사용자가 명시적으로 "코드
짜줘"/"구현해줘"라고 요청할 때만 예외.

CSV 데이터 파일, `scripts/main.py`, `app.py` 같은 인프라/배관
작업(새 파라미터를 `run_simulation()` 호출부에 연결, CSV 로딩,
분석/플로팅 스크립트)은 Claude가 직접 수정한다.
