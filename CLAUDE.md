# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트에서 세션을 시작할 때 항상 읽는
프로젝트 지시사항 파일이다. (Codex용 지시사항은 `AGENTS.md`에 있고,
두 파일은 같은 원칙을 따른다 - 한쪽을 고치면 다른 쪽도 맞출 것.)

## 기준 문서 (가장 중요)

**이 프로젝트의 기준은 GitHub 저장소(`Dorolong/WSC_Project`)에 있는
`.md` 문서와 커밋된 코드다.** 대화 기억은 컴퓨터와 세션을 따라오지
않으므로 절대 기준으로 삼지 않는다. 작업을 시작할 때 다음 순서로 읽는다:

1. `README.md` - 전체 요약(구조·구동원리·현재 상태·다음 할 일).
   여기가 항상 출발점이다.
2. `progress/` 폴더에서 **가장 큰 번호**의 파일(= 최신 백로그)의
   "다른 컴퓨터/세션에서 이어서 시작할 때 여기부터 읽을 것" 섹션.
3. 거기서 언급된 `progress/NN_주제.txt`(진행중 작업의 상세 설계/스펙).

읽은 내용을 종합해 지금 어디까지 됐고 다음에 뭘 해야 하는지 사용자에게
간단히 요약 보고한 뒤 바로 이어서 작업한다 - 사용자에게 다시 설명해
달라고 요청하지 않는다.

## Git 기반 작업 원칙

컴퓨터 간 인계는 **GitHub 저장소로만** 한다. ZIP 압축이나 별도의
"작업 환경 변경" 절차는 더 이상 쓰지 않는다.

1. 작업 시작 전 현재 브랜치와 작업 트리 상태를 확인하고, 필요하면
   원격 최신 상태를 확인한다(`git fetch`/`git status`).
2. 사용자가 지정한 브랜치·커밋이 있으면 그것을 기준으로, 없으면 현재
   체크아웃된 브랜치를 기준으로 작업한다.
3. 커밋되지 않은 사용자 변경사항을 임의로 덮어쓰거나 삭제하지 않는다.
4. 커밋·푸시·PR 생성은 사용자의 요청 범위 안에서만 수행한다.
5. **기능 구현이 끝나면 커밋·푸시까지 마친 뒤에 "완료했다"고 보고한다.**
   특히 "SQL 먼저 실행해달라"처럼 사용자 액션을 기다리는 중간 단계가
   있으면, 그 사이 다른 이슈가 끼어들어 push를 잊기 쉬우니 더 주의한다.
   (2026-08-02에 실제로 이 실수가 발생했다.)
6. 배포는 push 시 자동으로 이루어진다(Streamlit Cloud). 오라클 서버의
   Optuna 런처는 `git pull` + `systemctl restart`가 필요하다
   (`server/README.md`).

## 작업 기록 (매 작업 종료 시)

대화 내용에만 의존하지 않고 다음 세션이 저장소만 보고 이어갈 수 있도록
기록을 남긴다.

1. 완료된 작업마다 `progress/NN_주제.txt`를 새로 작성한다(기존 번호·
   스타일 확인 후 다음 번호 사용, 배경/구현/버그/검증/상태 구조).
2. 미완료 작업은 "무엇을", "어느 파일 몇 번째 줄부터", "왜 아직 안
   끝났는지"까지 구체적으로 남긴다.
3. 백로그("향후 개선 과제 백로그")를 갱신할 때는 **다음 번호로 새로
   저장하고 이전 백로그 파일은 삭제**한다. 백로그는 항상 `progress/`의
   **가장 큰 번호 하나만** 존재해야 한다(여러 세션이 각자 백로그를
   만들어 번호가 갈라진 적이 있으니, 갱신 전 현재 목록을 확인할 것).
4. 새로 발견했지만 안 고친 버그는 최신 백로그와 해당 파일 상태 문서
   (`progress/07`, `08` 등)에 "알려진 버그"로 명시한다.
5. 프로젝트 전체 상태나 할 일이 달라졌으면 `README.md`의 해당 섹션도
   같이 갱신한다(가장 먼저 읽히는 요약본이므로 반드시 동기화).
6. 상세한 문제 분석이 필요하면 `debug_logs/(YYYY-MM-DD)_주제.txt`에
   문제→원인→해결→상태 순으로 남긴다.
7. 기록에 비밀번호·토큰·개인키를 포함하지 않는다.

## 앱에 반영되는 변경은 버전을 올린다

사용자가 체감할 만한 기능을 배포할 때는 `app.py`의 `RELEASE_NOTES`
리스트 맨 앞에 항목을 추가하고 `APP_VERSION`이 가리키는 버전을 올린다
(현재 v1.0.8).

## 코드 작성 주체 (중요)

`Functions/Vehicle_Function.py`와 `mpc/mpc_controller.py`에 들어가는
의사결정/물리 로직은 사용자가 직접 작성한다. Claude는 공식/함수
시그니처/후크 지점 스펙을 텍스트로 제공하되, 이 두 파일에
Edit/Write를 직접 실행하지 않는다 - 사용자가 명시적으로 "코드
짜줘"/"구현해줘"라고 요청할 때만 예외.

CSV 데이터 파일, `scripts/main.py`, `app.py`, `server/` 같은 인프라/
배관 작업(새 파라미터를 `run_simulation()` 호출부에 연결, CSV 로딩,
분석/플로팅 스크립트, 서버/배포 설정)은 Claude가 직접 수정한다.

## 아키텍처 주의사항

- 설정값은 모듈 전역 싱글턴이 아니라 `cfg` -> `const`로 **인자 전달**
  한다. Streamlit Cloud는 여러 사용자가 서버 프로세스 하나를 공유해서,
  전역을 mutate하면 한 사용자의 설정이 모든 사용자에게 샌다(실제로
  두 번 발생, `progress/28`·`36`). `run_simulation()`/`mpc_speed()`를
  직접 호출하는 코드를 새로 쓸 때는 최신 시그니처를 반드시 확인한다.
- Optuna 탐색 공간 정의는 `scripts/main.py`의 `build_objective()`
  한 곳에만 둔다(웹 런처가 이걸 import해서 재사용).
