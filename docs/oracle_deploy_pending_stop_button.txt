[해소 2026-08-05] 서버 배포 확인(v1.3.0 라이브). 역할 종료.
================================================================
오라클 서버 반영 대기 메모 - Optuna Stop 버튼
================================================================

작성일: 2026-08-05

[ 현재 Git 상태 ]
- Stop 버튼 구현 커밋: e97cbc7 feat: add Optuna stop button
- GitHub origin/main push 완료.
- 로컬 main 과 origin/main 은 동기화됨.

[ 남은 일 ]
GitHub 에는 올라갔지만, 실제 서비스 https://wsc-drive.duckdns.org 에 반영하려면
오라클 서버에서 아래를 실행해야 한다.

    cd ~/WSC_Project
    git pull
    sudo systemctl restart wsc-launcher

[ 배포 후 확인 ]
- WSC_CHECKPOINT_EVERY=2 로 낮추고 Optuna trial 3~5 실행.
- Stop 버튼 클릭 후 status="stopping" -> "stopped" 로 바뀌는지 확인.
- Supabase optuna_runs 의 n_trials_completed 가 실제 완료 trial 수와 맞는지 확인.
- outputs/study_results 의 result JSON 이 생기는지 확인.
- outputs/studies 의 study sqlite/-wal/-shm 파일이 정리되는지 확인.
- Stop 없이 끝까지 실행하면 기존처럼 status="done" 이 되는지 확인.

[ 주의 ]
이 메모는 "push 필요"가 아니라 "서버 pull/restart 필요"를 남기기 위한 것이다.
push 자체는 e97cbc7 기준 이미 완료됐다.
================================================================
