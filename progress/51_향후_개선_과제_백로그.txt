================================================================
향후 개선 과제 백로그 (2026-08-05 기준 최신, progress/49 대체)
================================================================

[ 다른 컴퓨터/세션에서 이어서 시작할 때 여기부터 읽을 것 ]
1. README.md
2. 이 파일
3. docs/codex_security_tasks.txt

[ 최근 완료 ]
- 회원가입 모달: 사용자가 이미 해결했다고 확인. 코드에는
  server/static/index.html 의 signupDialog 와 server/static/js/auth.js 의
  Supabase signUp({ options: { data: { nickname } } }) 흐름이 존재한다.
- 서버 재시작 시 실행 상태 복원: 사용자가 이미 해결했다고 확인. 코드에는
  WSC_RUNS_STATE_PATH, _restore_runs_state(), _save_runs_state_unlocked()
  흐름이 존재한다.
- API 레이트 리밋: progress/50. 로컬 코드 반영 완료, 서버 배포/실기동
  검증은 아직 필요.

[ 지금 이어서 할 일 1순위 - 보안 후속 작업 ]
기준 문서: docs/codex_security_tasks.txt

1. 레이트 리밋 배포 검증
   - 서버에서 git pull + systemctl restart wsc-launcher 필요.
   - 정상 폴링/시뮬레이션/Optuna 가 429 없이 되는지 확인.
   - /api/status 를 한도 이상 빠르게 호출해 429 + Retry-After 확인.
   - WSC_RATELIMIT_ENABLED=0 킬 스위치 확인.
2. RLS/GRANT 전수 점검 + 스키마 SQL 문서화
   - docs/supabase_schema.sql 신설.
   - simulation_runs, user_settings, optuna_runs 의 create table/grant/RLS/policy
     를 코드 근거 파일:줄 주석과 함께 재실행 가능한 형태로 정리.
   - SETUP.md 의 "debug_logs 에서 검색하거나 Claude 에게 요청" 문구 제거.
   - progress/48 의 "SETUP.md 에 정리했다"는 부정확한 기록은 정정 문구를
     덧붙여 바로잡을 것.
3. Supabase 키 취급 정리
   - server/main.py, server/study_runner.py 의 SUPABASE_URL/ANON_KEY 기본값
     제거 여부 검토 및 환경변수 fail-fast 적용.
   - sync-config.js 는 브라우저 공개값임을 명시. service_role 금지.
4. 의존성 취약점 점검
   - pip-audit 결과를 표로 정리. 자동 업그레이드는 하지 말 것.
5. 백업 계획
   - outputs/study_results, progress/state, studies DB 등 무엇을 잃으면
     어떤 문제가 생기는지 표로 정리하고 제안까지만.
6. HSTS 1년 전환
   - 지금 하지 말 것. 인증서 자동 갱신 1회 성공 확인 후 판단.

[ 2순위 - MPC 물리 building block 완성 ]
Functions/Vehicle_Function.py 와 mpc/mpc_controller.py 의 의사결정/물리 로직은
사용자가 직접 작성한다. Codex 는 사용자가 명시적으로 구현 요청할 때만 편집.
- compute_downhill_cap(step, const) 구현.
- CS 접근 코스팅/제동 구현.
- 위가 끝나면 본격 Optuna 재탐색.

[ 3순위 - 문서/인프라 정리 ]
- SETUP.md 에 server/ 배포 절차 링크 추가.
- agent token 원격 Optuna 런처 구현: docs/agent_token_remote_optuna_guide.txt
  참고. server/main.py 를 건드리므로 보안 후속 작업과 순서 조율 필요.

[ 알려진 버그 ]
현재 코드 기준 새로 추적 중인 미해결 버그 없음.

[ 완료 이력 ]
- progress/13~17, 19~22, 24~26, 28, 30, 32, 34, 36, 38~41, 43, 45, 46, 48
  참고.
- progress/50: API 레이트 리밋 구현.
================================================================
