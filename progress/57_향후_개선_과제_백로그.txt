================================================================

[ 2026-08-06 갱신 ]
- 텔레메트리 Phase 1 배포 완료. 실배포에서 NaN 직렬화 버그를 잡았다
  (progress/54 하단). 업로드 정상 동작 확인됨.
- 전송 주기 정책 확정(progress/56). 10Hz 12개 / 1Hz 96개.
  SD카드는 32GB 이상 고내구 등급이면 충분(대회 전체 1.1GB).
- **다음: Phase 2(실시간 UDP 수신) 지시서 작성.** 착수 전 아래 둘을
  반드시 처리할 것:
  (1) **CAN ID 엔디안 검증** - telemetry/decode.py 의 parse_udp_packet()
      이 CAN ID 를 리틀엔디안으로 읽는데 **원본 Java 는 빅엔디안**이다
      (ByteBuffer 기본값). 원본은 2013년부터 실제 하드웨어로 검증된
      코드라 빅엔디안이 맞을 가능성이 높다. Phase 1 검증(87.74%)은
      CSV 경로라 이 함수를 전혀 타지 않아 **아직 아무도 확인 못 했다.**
      실제 UDP 패킷 1개를 양쪽으로 파싱해 specs/can_signals.csv 의
      아는 ID 108개와 대조하면 즉시 판별된다. 자동 테스트로 남길 것.
  (2) **서버 저장 전략** - 주기를 낮춰도 원본은 대회 전체 9GB 로
      Supabase 무료(500MB)·Pro(8GB) 둘 다 초과한다. 집계 저장이 필수.
      방향: 실시간 표시는 최근 구간만 / 분석용 1Hz 집계 / 원본은
      코스트다운 시험 같은 한정 구간만 요청 시. 상세는 progress/56.
- 그 외 Phase 2 신규 과제: 기기용 토큰(사람이 아닌 차량·중계기 인증,
  docs/agent_token_remote_optuna_guide.txt 설계 재사용), UDP 포트 개방
  (현재 방화벽 22/80/443만), 상시 수신기는 기존 대기열 패턴이 안 맞음,
  배치가 "N개 모이면"이 아니라 시간 기준이어야 함.
- 미해결로 남은 것: specs/can_signals.csv 가 108행인데 엑셀 명세는
  114개(BMS(HV) 60 vs 66, 6개 누락). MPPT #2·#3 14개는 우선순위 미기재.
향후 개선 과제 백로그 (2026-08-05 기준 최신, progress/53 대체)
================================================================

[다른 컴퓨터/세션에서 이어서 시작할 때 여기부터 읽을 것]
1. README.md
2. 이 파일
3. progress/54_CAN_텔레메트리_Phase1.txt
4. docs/codex_telemetry_tasks.txt
5. docs/codex_security_tasks.txt

[최근 완료]
- Optuna Stop 버튼: progress/52. 커밋/푸시 완료. Oracle 서버 git pull + restart 및
  V-a~V-h 실기동 검증은 아직 남아 있음.
- CAN 텔레메트리 Phase 1: progress/54. 업로드 API, 워커, 순수 파서, 신호 CSV,
  정적 UI, Supabase SQL 문서까지 구현. 로컬 Python/canlog/Supabase 부재로 실기동
  검증은 아직 남아 있음.

[지금 이어서 할 1순위]
1. Supabase에서 `docs/supabase_telemetry_phase1.sql` 실행
   - RLS와 GRANT가 포함돼 있음.
   - 실행 후 실제 계정으로 업로드 API가 401/403 없이 동작하는지 확인.
2. Oracle 서버 배포
   - `git pull`
   - `pip install -r server/requirements.txt`
   - `sudo systemctl restart wsc-launcher`
3. CAN 텔레메트리 Phase 1 실기동 검증
   - V-a: canlog.csv로 `scripts/verify_can_decode.py` reversed/LE 정확도 >=87.7%.
   - V-b: non-reversed/LE 정확도 <15%.
   - V-c: `telemetry_frames.raw_data_hex`가 보존되는지 DB에서 확인.
   - V-d: 웹 Telemetry 탭에서 업로드 -> 진행률 -> 완료 -> 내 로그 -> 차트 확인.
   - V-e: 1GB Oracle RAM에서 큰 로그 처리 중 메모리 급증이 없는지 확인.
   - V-f: 다른 계정 또는 직접 REST 호출로 타인 log_id 조회가 막히는지 확인.
   - V-g: 기존 Simulation/Optuna 실행이 깨지지 않았는지 확인.
4. Optuna Stop 버튼 실기동 검증
   - 이미 코드/푸시는 끝났지만 Oracle 서버 재시작 후 V-a~V-h를 아직 확인해야 한다.

[그 다음]
1. docs/codex_security_tasks.txt 작업 3 - Supabase 키 환경변수화
   - telemetry Phase 1과 같은 `server/main.py`를 건드리므로 telemetry 머지/배포 검증
     이후 착수.
2. docs/codex_security_tasks.txt 작업 2 - RLS/GRANT 전수 점검 + 스키마 SQL 문서화.
3. Phase 2 - 실시간 UDP 수신
   - `telemetry.decode.parse_udp_packet()`을 재사용하고, 수신부/버퍼/저장 경로만 추가.
   - Phase 1에서 만든 `telemetry_frames` 저장 구조를 그대로 쓸지, live session 테이블을
     분리할지 설계 필요.

[알려진 미검증/주의]
- 현재 컴퓨터에는 Python 실행기가 없어 V-a~V-g를 실제로 돌리지 못했다.
- 지시서에 언급된 canlog.csv 41,397행 파일은 저장소에 없다.
- `specs/can_signals.csv`의 확인 필요 항목:
  - BMS(HV) 0x040, 0x042, 0x123, 0x044, 0x046, 0x223: 3개 이상 필드.
  - Motor 전체 13행: VCU 확정 전 placeholder.
  - BMS(LV) 전체 14행: OrionBMS placeholder.
  - Motor 0x408, 0x417: reserved bytes 포함.
- `Functions/Vehicle_Function.py`와 `mpc/mpc_controller.py`는 계속 사용자 직접 작성 파일.
  이 백로그 작업에서도 수정 금지.

[완료 이력]
- progress/13~17, 19~22, 24~26, 28, 30, 32, 34, 36, 38~41, 43, 45, 46, 48 참고.
- progress/50: API 레이트 리밋 구현.
- progress/52: Optuna 탐색 Stop 버튼 구현.
- progress/54: CAN 텔레메트리 Phase 1 구현.
================================================================
