# AGENTS.md

이 파일은 Codex가 이 프로젝트에서 작업할 때 항상 따르는 프로젝트 지시사항이다.
(Claude Code용 지시사항은 `CLAUDE.md`에 있고, 두 파일은 같은 원칙을 따른다 -
한쪽을 고치면 다른 쪽도 맞출 것.)

## 기준 문서 (가장 중요)

이 프로젝트의 기준은 GitHub 저장소에 있는 `.md` 문서와 커밋된 코드다.
작업 시작 시 다음 순서로 읽는다:

1. `README.md` - 전체 요약(구조·구동원리·현재 상태·다음 할 일). 항상 출발점.
2. `progress/`에서 가장 큰 번호의 파일(= 최신 백로그)의 "다른 컴퓨터/세션에서
   이어서 시작할 때 여기부터 읽을 것" 섹션.
3. 거기서 언급된 `progress/NN_주제.txt`(진행중 작업의 상세 설계/스펙).

## Git 기반 작업 원칙

이 프로젝트의 작업 기준은 GitHub 저장소 `Dorolong/WSC_Project`의 최신 내용이다.
컴퓨터 간 ZIP 압축이나 별도의 "작업 환경 변경" 절차는 사용하지 않는다.

1. 작업을 시작하기 전에 현재 브랜치와 작업 트리 상태를 확인하고, 필요한 경우 원격 저장소의 최신 상태를 확인한다.
2. 사용자가 지정한 브랜치나 커밋이 있으면 그것을 기준으로 작업한다. 별도 지정이 없으면 현재 체크아웃된 브랜치를 기준으로 한다.
3. 기존의 사용자 변경사항과 커밋되지 않은 파일을 임의로 덮어쓰거나 삭제하지 않는다.
4. 원격 저장소에 영향을 주는 커밋, 푸시, PR 생성 등의 작업은 사용자의 요청 범위 안에서만 수행한다.

## 작업 기록 및 다음 작업 인계

매 작업이 끝날 때 대화 내용에만 의존하지 않고 다음 세션에서도 이어갈 수 있도록 저장소에 작업 기록을 남긴다.

1. 완료된 작업마다 `progress/NN_주제.txt` 형식의 새 파일을 작성한다. 기존 파일의 번호와 스타일을 확인하고 다음 번호를 사용하며, 배경/구현/버그/결과/상태를 기록한다.
2. 미완료 작업이 있으면 현재 진행 위치, 관련 파일과 줄 또는 함수, 남은 작업, 완료하지 못한 이유를 구체적으로 기록한다.
3. `progress/`의 최신 "향후 개선 과제 백로그"를 갱신할 필요가 있으면 다음 번호의 새 파일로 저장하고 이전 백로그 파일은 삭제한다. 백로그는 항상 `progress/`의 **가장 큰 번호 하나만** 존재해야 한다(여러 세션이 각자 백로그를 만들어 번호가 갈라진 적이 있으니 갱신 전 현재 목록을 확인할 것). 다음 세션이 바로 시작할 수 있도록 우선 작업을 명시한다.
4. 새로 발견했지만 고치지 않은 버그는 최신 백로그와 관련 상태 문서에 "알려진 버그"로 기록한다.
5. 프로젝트의 전체 상태나 할 일이 달라졌다면 `README.md`의 "현재 상태"와 "할 일"도 함께 갱신한다.
6. 상세한 문제 분석이 필요한 경우 `debug_logs/(YYYY-MM-DD)_주제.txt`에 문제→원인→해결→상태 순서로 남긴다.
7. 기록에는 비밀번호, 토큰, 개인키 등 비밀정보를 포함하지 않는다.

Codex는 대화 기록 자체를 직접 삭제할 수 없다. 따라서 각 작업 종료 후 필요한 맥락을 위 파일에 저장하고, 이후 작업에서는 대화 기억이 아니라 Git에 저장된 코드와 문서를 기준으로 판단한다.

## 코드 작성 주체 (중요)

`Functions/Vehicle_Function.py`와 `mpc/mpc_controller.py`에 들어가는
의사결정/물리 로직은 사용자가 직접 작성한다. Codex는 공식/함수
시그니처/후크 지점 스펙을 텍스트로 제공하되, 이 두 파일에
Edit/Write를 직접 실행하지 않는다. 사용자가 명시적으로 "코드
짜줘" 또는 "구현해줘"라고 요청할 때만 예외로 한다.

CSV 데이터 파일, `scripts/main.py`, `app.py`, `server/` 같은 인프라/
배관 작업(새 파라미터를 `run_simulation()` 호출부에 연결, CSV 로딩,
분석/플로팅 스크립트, 서버/배포 설정)은 Codex가 직접 수정한다.

## 앱에 반영되는 변경은 버전을 올린다

사용자가 체감할 만한 기능을 배포할 때는 `app.py`의 `RELEASE_NOTES`
리스트 맨 앞에 항목을 추가하고 `APP_VERSION`이 가리키는 버전을 올린다
(현재 v1.0.8).

## 아키텍처 주의사항

- 설정값은 모듈 전역 싱글턴이 아니라 `cfg` -> `const`로 **인자 전달**한다.
  Streamlit Cloud는 여러 사용자가 서버 프로세스 하나를 공유해서, 전역을
  mutate하면 한 사용자의 설정이 모든 사용자에게 샌다(실제로 두 번 발생,
  `progress/28`·`36`). `run_simulation()`/`mpc_speed()`를 직접 호출하는
  코드를 새로 쓸 때는 최신 시그니처를 반드시 확인한다.
- Optuna 탐색 공간 정의는 `scripts/main.py`의 `build_objective()` 한 곳에만
  둔다(웹 런처가 이걸 import해서 재사용).
